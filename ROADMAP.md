# Symbology roadmap

What this library encodes, what it is going to encode, and what it has decided
not to encode yet.

> These tiers are about **symbology coverage**. They are unrelated to the three
> execution tiers in `README.md` — extension, FFI, pure PHP — which are about
> how fast QR encodes and apply to nothing else.

## Shipped

Fifteen generators, each with a reference fixture generated by an encoder we did
not write and a decoder round trip. See `README.md` for what each one accepts.

| Family | Generators |
| --- | --- |
| Matrix | `qrcode`, `data-matrix`, `gs1-data-matrix` |
| Linear | `code128`, `gs1-128`, `code39`, `code39ext`, `code93`, `codabar`, `itf`, `itf14` |
| Retail | `ean13`, `ean8`, `upc-a`, `upc-e`, `ean2`, `ean5` |

## Tier 3 — the agreed scope, still open

This is what "complete" means for the v2 release. Nothing is tagged until these
are in.

- **GS1 layer, the rest of it.** `gs1-128` ships, and with it the derived
  application identifier table (`Generator\Gs1\ApplicationIdentifier`) and the
  FNC1 placement rule, which are the parts that are not specific to a
  symbology, and `gs1-data-matrix` follows it. What remains is GS1 QR, where
  FNC1 is a mode indicator rather than a symbol character or a codeword — which
  means touching QR's data encoder and deciding what the native backends do
  with it, since acceleration is QR-only and GS1 QR is still QR. Worth doing
  before the remaining symbologies, since every one added first is another
  place to retrofit it into.
- **Aztec** — reuses the parameterised GF(2^m) Reed–Solomon that QR and Data
  Matrix already share, with a different field per size. The cheapest of the
  three remaining symbologies; the spiral layout and the central finder are the
  new parts.
- **PDF417** — needs a **second Reed–Solomon implementation over GF(929)**. That
  is a prime field, so the existing GF(2^m) code does not generalise to it, and
  this is the largest single piece of work left. Plus 929 codeword patterns
  across three clusters and the text/numeric/byte compaction modes.
- **MaxiCode** — hexagons. `ModuleShape::Hexagon` exists in the model, but none
  of the four built-in renderers declares it, so this is two units of work: the
  generator, and hexagon support in at least the SVG and PNG renderers.
  Shipping the generator alone would mean shipping a symbology nothing can draw.

## Tier 4 — deferred

Decided 2026-09-03: real symbologies, deliberately out of scope for v2. Not
rejected — parked, so that finishing Tier 3 is a bounded job. Each is its own
unit of work if and when it is picked up.

- **GS1 DataBar** — Omnidirectional, Limited, Expanded. Genuinely common in
  retail and on coupons, and a symbology in its own right: nothing already here
  can be bent into it.
- **Four-state postal codes** — RM4SCC, KIX, Intelligent Mail, Australia Post.
  Part of the machinery is already written and unused:
  `AbstractRenderOptions::resolveRowHeights()` scales row heights
  proportionally precisely because a four-state code's meaning is the ratio
  between ascender, tracker and descender. The renderers are ready; there is no
  generator.
- **Micro QR and rMQR** — separate symbologies with their own version tables,
  not a mode of `qrcode`.
- **Minor linear codes** — MSI, Pharmacode, Code 11, Telepen. Cheap to add and
  correspondingly rare; the cost is API surface, not encoder complexity.

## Adding one

The bar is the same for every entry above, and it is in `AGENTS.md` under
*Verification Discipline*: a reference fixture from an independent encoder and a
decoder round trip are part of the work, not a follow-up. Where either cannot
cover something, the gap is declared with a test rather than left silent.
